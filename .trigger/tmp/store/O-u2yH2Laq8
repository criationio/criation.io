import {
  adSets,
  ads,
  and,
  campaigns,
  db,
  eq,
  gatewayEvents,
  isNull,
  sql,
  utmMappings
} from "../../../../chunk-LM5YBSEJ.mjs";
import {
  task
} from "../../../../chunk-4U6U2DH6.mjs";
import "../../../../chunk-TTPJKBBL.mjs";
import "../../../../chunk-OZGKWJ76.mjs";
import {
  logger
} from "../../../../chunk-6V3XLBYD.mjs";
import "../../../../chunk-4MZOQUDI.mjs";
import {
  __name,
  init_esm
} from "../../../../chunk-KXY2ZOOA.mjs";

// src/lib/trigger/tasks/stitch-gateway-event.ts
init_esm();

// src/lib/services/utm-stitcher.service.ts
init_esm();

// src/lib/db/queries/utm-matching.ts
init_esm();

// src/lib/services/utm-normalizer.ts
init_esm();
var META_LITERAL_REGEX = /\{\{[^}]+\}\}/;
function normalizeUtm(input) {
  if (input == null) return null;
  const trimmed = String(input).trim();
  if (trimmed.length === 0) return null;
  if (META_LITERAL_REGEX.test(trimmed)) return null;
  return trimmed.toLowerCase().normalize("NFD").replace(/[̀-ͯ]/g, "").replace(/[-_\s]+/g, "-").replace(/^-+|-+$/g, "");
}
__name(normalizeUtm, "normalizeUtm");
function isMetaLiteral(input) {
  if (input == null) return false;
  return META_LITERAL_REGEX.test(String(input));
}
__name(isMetaLiteral, "isMetaLiteral");

// src/lib/db/queries/utm-matching.ts
async function findCampaignByNormalizedName(workspaceId, utmCampaign, utmContent, utmTerm) {
  const normalizedCampaign = normalizeUtm(utmCampaign);
  if (!normalizedCampaign) return null;
  const candidates = await db.select({
    id: campaigns.id,
    name: campaigns.name
  }).from(campaigns).where(
    and(
      eq(campaigns.workspaceId, workspaceId),
      sql`lower(regexp_replace(unaccent(${campaigns.name}), '[-_\\s]+', '-', 'g')) = ${normalizedCampaign}`
    )
  ).limit(2);
  if (candidates.length === 0) return null;
  if (candidates.length > 1) return null;
  const campaign = candidates[0];
  const result = {
    campaignId: campaign.id,
    campaignName: campaign.name,
    matchedAdSetId: null,
    matchedAdId: null
  };
  const normalizedTerm = normalizeUtm(utmTerm ?? null);
  if (normalizedTerm) {
    const adSetMatches = await db.select({ id: adSets.id }).from(adSets).where(
      and(
        eq(adSets.campaignId, campaign.id),
        sql`lower(regexp_replace(unaccent(${adSets.name}), '[-_\\s]+', '-', 'g')) = ${normalizedTerm}`
      )
    ).limit(2);
    if (adSetMatches.length === 1) {
      result.matchedAdSetId = adSetMatches[0].id;
    }
  }
  const normalizedContent = normalizeUtm(utmContent ?? null);
  if (normalizedContent && result.matchedAdSetId) {
    const adMatches = await db.select({ id: ads.id }).from(ads).where(
      and(
        eq(ads.adSetId, result.matchedAdSetId),
        sql`lower(regexp_replace(unaccent(${ads.name}), '[-_\\s]+', '-', 'g')) = ${normalizedContent}`
      )
    ).limit(2);
    if (adMatches.length === 1) {
      result.matchedAdId = adMatches[0].id;
    }
  }
  return result;
}
__name(findCampaignByNormalizedName, "findCampaignByNormalizedName");
async function findManualMapping(workspaceId, utm) {
  const rows = await db.select({
    adId: utmMappings.adId,
    confidence: utmMappings.confidenceScore,
    utmSource: utmMappings.utmSource,
    utmMedium: utmMappings.utmMedium,
    utmCampaign: utmMappings.utmCampaign,
    utmContent: utmMappings.utmContent,
    utmTerm: utmMappings.utmTerm
  }).from(utmMappings).where(eq(utmMappings.workspaceId, workspaceId));
  if (rows.length === 0) return null;
  let bestMatch = null;
  for (const m of rows) {
    if (!m.adId) continue;
    const fields = [
      [m.utmSource, utm.source],
      [m.utmMedium, utm.medium],
      [m.utmCampaign, utm.campaign],
      [m.utmContent, utm.content],
      [m.utmTerm, utm.term]
    ];
    let matched = 0;
    let total = 0;
    let conflict = false;
    for (const [mappingValue, utmValue] of fields) {
      if (mappingValue == null) continue;
      total += 1;
      if (mappingValue === utmValue) matched += 1;
      else conflict = true;
    }
    if (conflict || total === 0 || matched < total) continue;
    const specificity = total;
    const confidence = Number(m.confidence ?? "1.0000");
    if (bestMatch === null || specificity > bestMatch.specificity) {
      bestMatch = { adId: m.adId, confidence, specificity };
    }
  }
  if (!bestMatch) return null;
  return { adId: bestMatch.adId, confidence: bestMatch.confidence };
}
__name(findManualMapping, "findManualMapping");
async function incrementCampaignAggregates(campaignId, amountCents, occurredAt) {
  const isWithin30d = Date.now() - occurredAt.getTime() < 30 * 24 * 60 * 60 * 1e3;
  await db.update(campaigns).set({
    revenueGrossCentsTotal: sql`${campaigns.revenueGrossCentsTotal} + ${amountCents}`,
    revenueGrossCents30d: isWithin30d ? sql`${campaigns.revenueGrossCents30d} + ${amountCents}` : sql`${campaigns.revenueGrossCents30d}`,
    attributedOrdersCount: sql`${campaigns.attributedOrdersCount} + 1`,
    lastStitchedAt: /* @__PURE__ */ new Date()
  }).where(eq(campaigns.id, campaignId));
}
__name(incrementCampaignAggregates, "incrementCampaignAggregates");
async function persistStitchResult(input) {
  await db.update(gatewayEvents).set({
    matchStrategy: input.strategy,
    matchedCampaignId: input.matchedCampaignId ?? null,
    matchedAdSetId: input.matchedAdSetId ?? null,
    matchedAdId: input.matchedAdId ?? null,
    matchConfidence: input.confidence != null ? input.confidence.toString() : null,
    stitchedAt: /* @__PURE__ */ new Date()
  }).where(and(eq(gatewayEvents.id, input.eventId), isNull(gatewayEvents.stitchedAt)));
}
__name(persistStitchResult, "persistStitchResult");

// src/lib/services/utm-stitcher.service.ts
async function stitchGatewayEvent(eventId) {
  const event = await db.query.gatewayEvents.findFirst({
    where: eq(gatewayEvents.id, eventId)
  });
  if (!event) {
    return {
      strategy: "unmatched",
      matchedCampaignId: null,
      matchedAdSetId: null,
      matchedAdId: null,
      confidence: null,
      reason: "event_not_found"
    };
  }
  if (event.stitchedAt) {
    return {
      strategy: event.matchStrategy,
      matchedCampaignId: event.matchedCampaignId,
      matchedAdSetId: event.matchedAdSetId,
      matchedAdId: event.matchedAdId,
      confidence: event.matchConfidence ? Number(event.matchConfidence) : null,
      reason: "already_stitched"
    };
  }
  const utmSet = {
    source: event.utmSource,
    medium: event.utmMedium,
    campaign: event.utmCampaign,
    content: event.utmContent,
    term: event.utmTerm
  };
  const manual = await findManualMapping(event.workspaceId, utmSet);
  if (manual) {
    const adRow = await db.query.ads.findFirst({
      where: eq(ads.id, manual.adId)
    });
    let campaignId = null;
    let adSetId = null;
    if (adRow) {
      adSetId = adRow.adSetId;
      const adSetRow = await db.query.adSets.findFirst({
        where: eq(adSets.id, adRow.adSetId)
      });
      campaignId = adSetRow?.campaignId ?? null;
    }
    const result2 = {
      strategy: "manual",
      matchedCampaignId: campaignId,
      matchedAdSetId: adSetId,
      matchedAdId: manual.adId,
      confidence: manual.confidence
    };
    await persistStitchResult({ eventId, ...result2 });
    return result2;
  }
  if (isMetaLiteral(event.utmSource) || isMetaLiteral(event.utmMedium) || isMetaLiteral(event.utmCampaign) || isMetaLiteral(event.utmContent) || isMetaLiteral(event.utmTerm)) {
    const result2 = {
      strategy: "meta_literal",
      matchedCampaignId: null,
      matchedAdSetId: null,
      matchedAdId: null,
      confidence: null,
      reason: "utm_contains_unresolved_meta_placeholder"
    };
    await persistStitchResult({ eventId, ...result2 });
    return result2;
  }
  if (event.utmCampaign) {
    const campaignMatch = await findCampaignByNormalizedName(
      event.workspaceId,
      event.utmCampaign,
      event.utmContent,
      event.utmTerm
    );
    if (campaignMatch) {
      const result2 = {
        strategy: "perfect",
        matchedCampaignId: campaignMatch.campaignId,
        matchedAdSetId: campaignMatch.matchedAdSetId,
        matchedAdId: campaignMatch.matchedAdId,
        confidence: 1
      };
      await persistStitchResult({ eventId, ...result2 });
      return result2;
    }
  }
  const result = {
    strategy: "unmatched",
    matchedCampaignId: null,
    matchedAdSetId: null,
    matchedAdId: null,
    confidence: null,
    reason: event.utmCampaign ? "no_campaign_name_match" : "no_utm_campaign_provided"
  };
  await persistStitchResult({ eventId, ...result });
  return result;
}
__name(stitchGatewayEvent, "stitchGatewayEvent");
async function stitchAndAggregate(eventId) {
  const result = await stitchGatewayEvent(eventId);
  if (result.matchedCampaignId && (result.strategy === "perfect" || result.strategy === "manual")) {
    const event = await db.query.gatewayEvents.findFirst({
      where: eq(gatewayEvents.id, eventId)
    });
    if (event && event.amountCents && event.amountCents > 0 && isRevenueEvent(event.eventType)) {
      await incrementCampaignAggregates(
        result.matchedCampaignId,
        event.amountCents,
        event.creationDateMs ? new Date(event.creationDateMs) : event.createdAt
      );
    }
  }
  return result;
}
__name(stitchAndAggregate, "stitchAndAggregate");
var REVENUE_EVENT_TYPES = /* @__PURE__ */ new Set([
  "PURCHASE_APPROVED",
  "PURCHASE_COMPLETE",
  "SUBSCRIPTION_RENEWED"
]);
function isRevenueEvent(eventType) {
  return REVENUE_EVENT_TYPES.has(eventType);
}
__name(isRevenueEvent, "isRevenueEvent");

// src/lib/trigger/tasks/stitch-gateway-event.ts
var stitchGatewayEventTask = task({
  id: "stitch-gateway-event",
  maxDuration: 30,
  retry: { maxAttempts: 3, factor: 2, minTimeoutInMs: 1e3, maxTimeoutInMs: 1e4 },
  run: /* @__PURE__ */ __name(async (payload) => {
    const { eventId, workspaceId } = payload;
    logger.info("stitch-gateway-event start", { eventId, workspaceId });
    try {
      const result = await stitchAndAggregate(eventId);
      logger.info("stitch-gateway-event done", {
        eventId,
        strategy: result.strategy,
        matchedCampaignId: result.matchedCampaignId,
        confidence: result.confidence
      });
      return { ok: true, ...result };
    } catch (err) {
      const errorMessage = err instanceof Error ? err.message : String(err);
      logger.error("stitch-gateway-event failed", { eventId, err: errorMessage });
      throw err;
    }
  }, "run")
});
export {
  stitchGatewayEventTask
};
//# sourceMappingURL=stitch-gateway-event.mjs.map
