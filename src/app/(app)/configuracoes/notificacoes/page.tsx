import { PagePlaceholder } from '@/components/shell/PagePlaceholder'

export default function ConfigNotificacoesPage() {
  return (
    <PagePlaceholder
      title="Notificações"
      description="Email, push e in-app — escolha o que recebe."
      session="Sessão 2.8"
    />
  )
}
