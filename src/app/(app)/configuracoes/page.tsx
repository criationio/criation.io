import { PagePlaceholder } from '@/components/shell/PagePlaceholder'

export default function ConfiguracoesPage() {
  return (
    <PagePlaceholder
      title="Configurações"
      description="Conexões, gateways, CAPI, equipe, notificações, faturamento, API e segurança."
      session="Sessões 1.3 → 1.13"
    />
  )
}
