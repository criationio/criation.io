import { PagePlaceholder } from '@/components/shell/PagePlaceholder'

export default function AfiliadosPage() {
  return (
    <PagePlaceholder
      title="Afiliados"
      description="Programa de afiliados — links únicos, rastreio e comissões."
      session="Sessão 3.6"
    />
  )
}
