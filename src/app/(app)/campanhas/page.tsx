import { PagePlaceholder } from '@/components/shell/PagePlaceholder'

export default function CampanhasPage() {
  return (
    <PagePlaceholder
      title="Campanhas"
      description="Lista unificada de campanhas Meta + Google enriquecidas com receita real do gateway."
      session="Sessão 1.7"
    />
  )
}
