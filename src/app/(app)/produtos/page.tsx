import { PagePlaceholder } from '@/components/shell/PagePlaceholder'

export default function ProdutosPage() {
  return (
    <PagePlaceholder
      title="Produtos"
      description="Mapeamento entre produtos do gateway e produtos do app."
      session="Sessão 2.13"
    />
  )
}
