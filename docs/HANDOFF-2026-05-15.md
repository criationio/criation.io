# 🤝 HANDOFF — Pós sessão 1.4.9.B (2026-05-15)

**Para:** Próxima sessão Claude (terminal novo)
**De:** Sessão anterior — Claude Opus 4.7 + Vinicius
**Status do projeto:** 1.4.9.B (Google EC fanout) fechada com audit. 1.4.9.C (drop click_id_store) fechada. Próximo gate: 1.4.9.5 (shadow validation, bloqueada por deps externas).

## TL;DR do que precisa ser feito agora

**1 ação urgente, 3 confirmações:**

1. **[URGENTE] Rotacionar `GOOGLE_ADS_DEVELOPER_TOKEN`** (vazou em commit `9078a4b` antes do redact `eebf073`; sem rotação não pode `git push`)
2. **Atualizar `.env.local`** com novo token
3. **Re-deploy Trigger.dev** com novo token (syncEnvVars empurra automático)
4. **Confirmar via `trigger env list --env prod`** que o valor mudou

Depois disso, branch fica safe pra `git push origin develop` (20 commits ahead).

## Comandos exatos

### 1. Rotacionar token no Google Ads

Não tem CLI pra isso. Caminho manual:

```
https://ads.google.com  (logado como me@heywhispa.com)
→ Tools & Settings (chave inglesa, canto superior direito)
→ Setup → API Center
→ Botão "Reset developer token"
→ Confirmar → novo token aparece em até 5s
→ COPIAR
```

> ⚠️ Token antigo (valor atual em `.env.local` linha `GOOGLE_ADS_DEVELOPER_TOKEN`) fica invalidado imediato. Se tem fanout rodando contra contas reais, vai começar a falhar antes do redeploy. Tudo bem nesse caso (workspace `heywhispa` está em dev/test mode, não está em produção real ainda).

### 2. Atualizar `.env.local`

```bash
# Editor de preferência. Trocar o valor existente da linha:
#   GOOGLE_ADS_DEVELOPER_TOKEN=<antigo>
# pelo novo token copiado do API Center:
#   GOOGLE_ADS_DEVELOPER_TOKEN=<novo_token_copiado>
```

### 3. Re-deploy Trigger.dev (sincroniza token novo pro cloud)

```bash
# IMPORTANTE: macOS sem Docker Desktop precisa do workaround DOCKER_CONFIG.
# Memory `trigger_dev_deploy_gotchas_2026_05.md` tem detalhes.
mkdir -p /tmp/docker-clean
echo '{"auths":{}}' > /tmp/docker-clean/config.json
DOCKER_CONFIG=/tmp/docker-clean pnpm trigger:deploy
```

Esperado:

- ✅ `Successfully deployed version 20260515.X`
- ✅ `15 detected tasks` (era 13 antes da 1.4.9.B step 12; +2 do google-token-refresh)

### 4. Confirmar sincronização

```bash
pnpm exec trigger env list --env prod | grep DEVELOPER
# Deve listar:
# GOOGLE_ADS_DEVELOPER_TOKEN  ******  (valor mascarado, mas confirma que existe)
```

Pra ver o valor não-mascarado:

```bash
pnpm exec trigger env get GOOGLE_ADS_DEVELOPER_TOKEN --env prod
# Compara com .env.local — devem ser iguais
```

## Validação opcional pós-rotation

### Smoke test: cron `fanout-google-data-manager-pickup-cron` continua passando

Crons rodam a cada 10min. Após o redeploy (~5min depois), checar:

```bash
# Via Trigger.dev MCP:
mcp__trigger__list_runs(
  environment: 'prod',
  filterTaskIdentifier: 'fanout-google-data-manager-pickup-cron',
  period: '15m',
  limit: 3
)
# Esperado: completed (não failed), output { triggered: 0 } (workspace vazio)
```

Se rodar `failed`, conferir env var sincronizou e olhar `get_run_details`.

## Como pushar pra origin/develop com segurança

Depois da rotation:

```bash
git log --oneline origin/develop..HEAD | wc -l
# Esperado: 21 commits ahead (20 anteriores + 1 desse handoff doc se commitar)

git push origin develop
```

> 💡 Mesmo com o token rotacionado, o histórico git local ainda tem o valor antigo em `git show 9078a4b`. Esse valor agora é **inválido** (rotation invalidou), então mesmo se alguém visse o commit antigo, não funcionaria. Por isso a rotation é equivalente a `git filter-branch` sem precisar reescrever história.

## Estado do projeto

### Que ficou pronto na 1.4.9.B

| Camada                          | Status | Arquivos chave                                                                                |
| ------------------------------- | ------ | --------------------------------------------------------------------------------------------- |
| ADR + schema                    | ✅     | `docs/adr/ADR-015-plataforma-google-2026.md`, migration `0016_google_data_manager.sql`        |
| Hashing                         | ✅     | `src/lib/services/capi/hashing.ts` (namespace `hashForGoogleDataManager` + 21 testes)         |
| OAuth flow                      | ✅     | `src/app/api/oauth/google/callback/route.ts`, `src/lib/actions/google-connections.ts`         |
| REST client metadata            | ✅     | `src/lib/services/google-ads-metadata.service.ts`                                             |
| Adapter Data Manager            | ✅     | `src/lib/services/capi/google.adapter.ts` (18 testes)                                         |
| Service orchestrator            | ✅     | `src/lib/services/capi/google.service.ts`                                                     |
| Trigger task + cron pickup      | ✅     | `src/lib/trigger/tasks/fanout-google-data-manager.ts`                                         |
| Wiring process-tracking-event   | ✅     | `src/lib/trigger/tasks/process-tracking-event.ts`                                             |
| Wizard UI                       | ✅     | `src/app/(app)/configuracoes/google/conversoes/` (page + client + 4 actions)                  |
| `/tracking` aba Google          | ✅     | `src/app/(app)/tracking/page.tsx` + `src/lib/db/queries/capi.ts` (getGoogleFanoutStats)       |
| Token refresh cron diário       | ✅     | `src/lib/trigger/tasks/google-token-refresh.ts` + `src/lib/services/token-refresh.service.ts` |
| PII redact paths Google         | ✅     | `src/lib/logger.ts` (REDACT_PATHS + 2 testes)                                                 |
| Audit fixes (P1+P2+P3)          | ✅     | commit `e6e90f2` (4 P1 + 4 P2 + 1 P3)                                                         |
| TDs novos (TD-113..119)         | ✅     | `docs/tech-debt.md`, commit `9772b28`                                                         |
| Drop `click_id_store` (1.4.9.C) | ✅     | migration `0017_drop_click_id_store.sql` (aplicada em prod), commit `22dfbf5`                 |

### Suite

- **392/392** testes passando (Vitest)
- **Typecheck:** zero erros
- **Build local:** não validado (não precisava nesta sessão)

### Trigger.dev cloud

- Project: `proj_xxaeizypavwtbpfpzyzk`
- Env: `prod` (sincronizado via `syncEnvVars` extension)
- Versão atual deploy: `v20260515.6`
- 15 tasks ativas (Meta + Google + processo + tokens + 4 crons)
- 17 env vars do projeto + 12 OTEL/heartbeat auto-injetadas = 29 total

### DB prod

- 1 google_connection ativa: `me@heywhispa.com` (smoke test, accounts=0 por Test access limitation)
- `click_id_store` table dropada (confirmado via `pg_tables` query)
- 0 capi_events ainda (nada de tráfego real fluindo)

## Próximos passos (depois da rotation)

### Curto prazo (essa semana)

- [ ] Confirmar rotation + push pra origin/develop
- [ ] Marcar [[urgent-rotate-google-developer-token]] memory como resolvida (deletar arquivo + entry no MEMORY.md)
- [ ] Começar **caminho crítico** da 1.4.9.5 prep:
  - [ ] Verificar `criation.io` no Search Console (5min + DNS)
  - [ ] Aplicar Developer Token **Basic access** no API Center MCC (formulário 5 min, resposta 1-2 dias úteis)

### Médio prazo (2-6 semanas)

- [ ] Quando `criation.io` verificado + páginas `/privacy` + `/terms` reais publicadas:
  - [ ] Submeter **OAuth verification** Google (GCP Console → OAuth consent screen → Publish App)
  - [ ] Gravar demo video + escrever justificativas dos 3 sensitive scopes
- [ ] Deploy app Vercel `gru1` com env vars de prod completas
- [ ] Identificar cliente alpha (AGC interno ou amigo)

### Quando 5 deps externas estiverem prontas

- [ ] Abrir sessão **1.4.9.5 — Shadow validation E2E em prod real** (ROADMAP linha 282)
- [ ] Definição de pronto: zero P0, EMQ Meta ≥7 após 7d, Google EC match rate ≥60% após 7d, GATE 1.4.8 + TD-104 verdes, decisão go/no-go pra 1.6

## Arquivos pra ler primeiro na nova sessão

1. `~/.claude/projects/-Users-viniciusbenavides-App-Criation-Io/memory/MEMORY.md` (índice de memórias)
2. Este arquivo (`docs/HANDOFF-2026-05-15.md`)
3. `docs/checklists/1.4.9.5-PREP.md` (dependências externas)
4. `ROADMAP.md` linhas 22-28 (estado geral) + linhas 278-282 (1.4.9.B + 1.4.9.C + 1.4.9.5)
5. `docs/tech-debt.md` TD-113..TD-119 (audit findings deferidos)

## Notas finais

- **Branch `develop`** está em `eebf073` (HEAD), à frente de `origin/develop` por 20 commits
- **Não pushar** antes de fazer rotation do developer token
- **Dev server local** pode estar rodando em background da sessão anterior — `pkill -f "next dev"` se quiser limpo
- **Tunnel cloudflared** estava marcado pra usar pra webhooks (`NEXT_PUBLIC_APP_URL=https://mail-cognitive-carbon-perception.trycloudflare.com`), mas precisa reiniciar manualmente porque é quick-tunnel ephemeral (URL muda a cada subida)

---

**Boa sessão.** Se tiver dúvida, este doc + memórias persistentes em `~/.claude/projects/` cobrem o contexto necessário.
