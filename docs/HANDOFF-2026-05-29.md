# 🤝 HANDOFF — 2026-05-29

**Para:** Próxima sessão Claude (continuar amanhã)
**De:** Sessão Claude Opus 4.8 + Vinicius — 2026-05-29 (sessão longa, ~18 commits)
**Status do projeto:** **Trilha do Estúdio Analisar fechada (1.9→1.11)** + **sync de criativos** + **refinamentos de UX** + **renomeação de planos** + **1.12 billing backend (Asaas/Stripe + webhooks + allocate)**. Suite **675/676**, tudo em `develop` pushado, Trigger prod `20260529.6`.

## TL;DR

Dia muito produtivo. Entregamos o primeiro pipeline real do Estúdio fim-a-fim e todo o polish ao redor dele, mais o backend de cobrança da Criation:

- **1.9** `analisar.video_ad` (Quick) — UI `/estudio/analisar/nova` (cascata) → action → Trigger.dev → claude.service → resultado.
- **1.10** polling automático + typewriter (a página de resultado atualiza sozinha).
- **1.11** página de resultado seccionada + footer com saldo + export (PDF/DOCX/JSON).
- **Sync de criativos** (gap da 1.7): `ad_creatives` agora popula com copy/thumbnail reais.
- **Refinamentos UX** do Estúdio: seletor em cascata (conta→campanha→anúncio + nome exato Meta), nomear/renomear/apagar análise, pastas (criar/mover/filtrar/apagar).
- **2 fixes** de prod: loop dashboard↔bem-vindo (cookie de onboarding sumido) + cor de texto transparente em Dialog/Input.
- **Renomeação de planos**: Starter/Pro/Agency → **Pro / Advanced / Enterprise** + catálogo único `src/lib/billing/plans.ts`.
- **1.12** billing.service (Asaas completo + Stripe enxuto) + webhooks + `process-billing-event` → allocate.

## ⏭️ O QUE FAZER AMANHÃ (ação imediata)

**Terminar o E2E sandbox da 1.12** (bloqueado só por config). O código está pronto; falta:

1. **Vinicius cola as chaves do Asaas sandbox no `.env.local`** (não tem no ambiente hoje):
   ```
   ASAAS_API_KEY=<chave de API do Asaas sandbox>
   ASAAS_BASE_URL=https://api-sandbox.asaas.com/v3
   ASAAS_WEBHOOK_SECRET=<token livre, ex: whsec_criation_dev_xyz>
   ```
2. **Claude:** sobe `pnpm dev` + túnel `cloudflared` (a preview Vercel é bloqueada por SSO pra webhooks de servidor — E2E vai por túnel local, padrão do projeto — ver `memory/reference_webhook_e2e_setup`).
3. **Claude:** registra o webhook no Asaas sandbox via API (`POST /v3/webhooks`, URL do túnel `…/api/webhooks/asaas`, header token = `ASAAS_WEBHOOK_SECRET`, eventos PAYMENT\_\*).
4. **Vinicius cria a assinatura teste** (painel Asaas sandbox ou Claude dispara `createSubscription`).
5. **Validar:** webhook chega → `processed_webhook_events` registra → `process-billing-event` aloca crédito → `subscriptions` + `workspaces.plan_id` atualizados. Reenviar o MESMO webhook → não duplica (idempotência).

**Depois:** **1.13 — Checkout transparente (Pix + cartão tokenizado) + paywalls + troca de plano** (consome `billing.service.createSubscription` do Asaas, já pronto). Boleto a decidir.

## O que foi feito (detalhe)

### Estúdio Analisar (1.9 → 1.11)

- **1.9** (`166ddfc`): `analysis.service` (`detectBottleneckHint` heurística + `buildBlocoTransicao` + `runVideoAdAnalysis`), task `estudio-analisar-video-ad`, action `createAnalysis` (thin, preflight saldo), page `[id]`. Validado E2E real (Claude Sonnet + consumo atômico de crédito).
- **1.10** (`a6c1d43`): `QueryProvider` (1ª adoção real do TanStack Query) + action `getAnalysisStatus` + `RunningWatcher` (polling 2.5s → `router.refresh()`) + `Typewriter`.
- **1.11** (`98b157e`): `analysis_results.input_snapshot` (snapshot do BLOCO) + página seccionada (header executivo/resumo/diagnóstico/fortes-fracos/sugestões P1-P3/dados do funil/footer saldo).
- **Export** (`a0b18dd`): botão "Baixar" → PDF (`@react-pdf/renderer`) / DOCX (`docx`) / JSON, libs sob demanda (dynamic import).

### Sync de criativos (`4349d9f`)

`meta.service` expande `creative{...}` no edge de ads (zero chamadas extras) + `extractCreativeContent` (ladder object_story_spec/asset_feed_spec) + `upsertCreative` + wiring. Validado real: 3613 criativos / 93% com copy.

### Refinamentos UX do Estúdio

- Seletor **cascata** conta→campanha→anúncio + label = nome exato do Meta (`7985188`).
- Nomear/renomear/apagar análise (`335a63f`) — apagar com diálogo de confirmação.
- **Pastas** planas, do workspace (`57c4ece`) — criar/renomear/apagar + mover (menu ⋮) + filtro `?folder=`.

### Fixes de prod

- **Loop dashboard↔bem-vindo** (`cac414b`): cookie `criation_onboarding_done` sumido + DB 'completed' → loop. Fix: route handler `GET /api/onboarding/enter` reconstrói o cookie.
- **Cor transparente** em DialogTitle/Input no portal Radix (`c2c9ecb`): `text-[var(--color-fg)]` explícito.

### Renomeação de planos (`6a5663a`)

**Pro** R$297/120cr · **Advanced** R$497/300cr · **Enterprise** R$997/700cr (seats 1/3/10). Catálogo único `src/lib/billing/plans.ts`. Remap posicional (starter→pro, pro→advanced, agency→enterprise). budget.ts (Regra 20) remapeado: pro R$10/advanced R$40/enterprise R$120. Margem floor 84/76/72%. DB tinha só 'free' (nada a migrar). **`plan_id` fica em `workspaces`+`subscriptions`, NÃO em users.**

### 1.12 billing (`564df59`→`f37a625`)

Billing **merchant** (Criation vende; ≠ webhooks de gateway do cliente).

- `billing/types.ts` (BillingEvent normalizado + BillingAdapter).
- `asaas.adapter` **completo** (REST `access_token`, base sandbox, createSubscription transparente Pix/cartão tokenizado, cancel/changePlan/refund, validateWebhook `asaas-access-token`, parseEvent). Valor em **reais**. `externalReference="${ws}|${plan}"` ou `|pack:${sku}`.
- `stripe.adapter` **enxuto** (constructEvent + parseEvent reais; create = NOT_IMPLEMENTED → 1.13).
- `billing.service` (roteamento BR→asaas/lock ADR-007 + handleWebhook).
- rotas `/api/webhooks/asaas|stripe` (`runtime='nodejs'`, validate→dedup `processed_webhook_events`→enfileira→200/401).
- task `process-billing-event` → `applyBillingEvent`: invoice_paid→upsertSubscription+setWorkspacePlan+allocate('subscription'); pack→allocate('pack'); refund→creditService.refund; cancel→marca (sem revogar).
- **Idempotência 2 camadas:** processed_webhook_events + idempotencyKey=id do evento (allocate/refund já idempotentes).
- +31 testes.

## Quality gates — todos verdes

| Check               | Final                                      |
| ------------------- | ------------------------------------------ |
| `pnpm tsc --noEmit` | ✅                                         |
| `pnpm lint`         | ✅                                         |
| `pnpm test`         | **675/676** (1 skip = integração créditos) |
| `pnpm build`        | ✅                                         |

## Estado de deploy

- **develop**: pushado até `7bcf510` (18 commits hoje). Preview Vercel rebuildando a cada push.
- **Trigger.dev prod**: `20260529.6`, **20 tasks** (inclui `estudio-analisar-video-ad` + `process-billing-event`). `ANTHROPIC_API_KEY` sincronizada.
- **Supabase** (projeto único `kxcljhjnpizznzdcgiyt` — dev/preview/prod compartilham): DDL aditivo aplicado via mcp (`analyses.name`, `analyses.folder_id`, `analysis_folders`, `analysis_results.input_snapshot`).
- **main/prod**: NÃO tem nada de hoje ainda (tudo em develop). Promover quando validado.

## Pendências / gotchas

- **1.12 config Vercel + dashboards (pra prod):** setar `ASAAS_*`/`STRIPE_*` na Vercel (Prod+Preview) + registrar webhooks de produção quando promover develop→main. Pra testar agora = túnel local (ver "amanhã").
- **GATE pré-prod aberto:** tracking install em 1 landing real (receita/ROAS zerados → toda análise diagnostica "landing").
- **TD sync de criativos:** full sync da agência (32 contas) leva >34min vs `maxDuration` 600s — backfill pode precisar de múltiplos runs do cron (idempotente; não bloqueia).
- **Migrations Drizzle mortas após 0006** — schema via `db:push`/DDL direto (mcp supabase). `db:generate` drifted. Não usar generate.
- **1.4.9.5** shadow validation segue bloqueada por OAuth Google (rework de vídeo).
- Vinicius vai rotacionar a ANTHROPIC key (foi colada no chat) e revogar a antiga.

## Gotchas técnicos reusáveis (desta sessão)

1. Remap posicional de enum (plano) — find-replace cego erra; editar com contexto; no DB ordenar UPDATEs anti-colisão.
2. Trigger deploy macOS: `DOCKER_CONFIG=<dir limpo {"auths":{}}>` antes de `pnpm trigger:deploy`.
3. Script TS fora do Next: `node --env-file=.env.local --import tsx script.ts` (t3-env valida no import; `loadEnvFile` no topo é tarde).
4. Preview Vercel tem **SSO (Deployment Protection)** que bloqueia webhooks de servidor → E2E de webhook vai por túnel local. Usar branch alias (não URL com hash) pra browser.
5. lint react-hooks estrito: reset de state via `key`, não setState-in-effect nem ref-in-render.
6. Asaas: valor em reais (÷100); auth header `access_token`; webhook valida `asaas-access-token`; `externalReference` volta nos eventos (resolve workspace/plano).

## Commits da sessão (18)

```
7bcf510 docs(roadmap): 1.12 billing entregue
f37a625 feat(billing): 1.12 C — webhooks + process-billing-event
de425dc feat(billing): 1.12 B — adapters Asaas/Stripe + billing.service
564df59 feat(billing): 1.12 A — types + queries + env + dep stripe
6a5663a refactor(billing): renomeia planos Pro/Advanced/Enterprise + catálogo
a0b18dd feat(estudio): export multi-formato (PDF/DOCX/JSON)
cac414b fix(auth): loop dashboard↔bem-vindo (cookie onboarding)
50690fa docs(roadmap): 1.11 entregue
98b157e feat(estudio): 1.11 resultado seccionado + footer saldo
c2c9ecb fix(ui): cor texto DialogTitle/Input no portal
57c4ece feat(estudio): pastas de análise
335a63f feat(estudio): nomear/renomear/apagar análise
7985188 feat(estudio): seletor cascata + nome exato Meta
b70d546 docs(roadmap): 1.10 entregue
a6c1d43 feat(analysis): 1.10 polling + typewriter
53c026f docs(roadmap): sync de criativos + TD duração
4349d9f feat(meta): sync de criativos
166ddfc feat(analysis): 1.9 analisar.video_ad + /estudio/analisar/nova
```

## Trigger.dev deploys

```
20260529.6 | 20 tasks | + process-billing-event (1.12)
20260529.5 | 19 tasks | sync-campaigns com creative sync
20260529.4 | 19 tasks | + estudio-analisar-video-ad (1.9) + ANTHROPIC_API_KEY
```

---

**Como retomar amanhã:** `git status` (develop clean) → pedir as chaves Asaas sandbox no `.env.local` → túnel + registro webhook → Vinicius cria sub teste → validar allocate idempotente → seguir pra 1.13 (checkout).
